name: shared
description: Pure Dart domain models and rules shared by QuickServe applications.
publish_to: "none"
version: 0.0.1

environment:
  sdk: ^3.13.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
